version: '3'

services:
  app:
    build: .
    image: opalmedapps/registration
    ports:
      - 8083:80
    volumes:
      - $PWD:/var/www/html/
      # don't overwrite existing content in these directories
      - /var/www/html/node_modules
      - /var/www/html/bower_components
